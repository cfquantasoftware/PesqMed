{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "PesqMed: menu (primeira tela)",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/menu.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": { "PYTHONPATH": "${workspaceFolder}/src" }
        }
    ]
}
